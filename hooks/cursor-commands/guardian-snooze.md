# Snooze gates (15m)

Type **`/guardian-snooze`** here, or in Terminal:

```bash
~/.guardian/guardian snooze 15
```

**macOS:** open `~/.guardian/Guardian-Snooze-15m.command` (double-click or `open`).

Then send your prompt again.
